# Prototype: manpages corpus with repo-local man tree + Go source files.

[manpath]
include = ["build/man"]
no-auto = true

[[corpora]]
type = "manpages"

[[corpora]]
name = "maneater-src"
type = "files"
paths = ["cmd/maneater/*.go", "internal/embedding/*.go"]
max-chars = 1000
